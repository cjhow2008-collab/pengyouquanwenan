{
    "framework": "vite",
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "rewrites": [
        {
            "source": "/zhipu-api/:path*",
            "destination": "https://open.bigmodel.cn/api/:path*"
        }
    ]
}